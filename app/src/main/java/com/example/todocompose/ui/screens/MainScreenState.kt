package com.example.todocompose.ui.screens

class MainScreenState {
    companion object {
        val EMPTY = MainScreenState(
        )
    }
}

// we need to keep track of:
// whether an item is checked or not
// the items that are currently displayed onscreen (this might be a database??)